---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const latestArticles = (await getCollection('docs'))
  .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime())
  .slice(0, 3);
---

<Layout title="ようこそ！">
  <div class="min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
      <img
        class="mx-auto h-24 w-auto rounded-full shadow-lg"
        src="/mypage/icon.png"
        alt="プロフィール画像"
      />
      <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
        Yuji's Personal Page
      </h2>
      <p class="mt-2 text-center text-sm text-gray-600">
        ようこそ！データエンジニアリング、開発、Rustに関する私の知識や考察を共有しています。
      </p>
    </div>

    <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-xl">
      <!-- GitHub Contributions Chart -->
      <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10 mb-8">
        <h3 class="text-2xl font-bold text-gray-900 mb-6 text-center">GitHub Contributions</h3>
        <img
          class="mx-auto w-full max-w-lg rounded-lg shadow-md"
          src="https://ghchart.rshah.org/yujikawa"
          alt="GitHub Contributions Chart"
        />
        <p class="mt-2 text-center text-xs text-gray-500">
          <small>この画像は <a href="https://ghchart.rshah.org/" target="_blank" rel="noopener noreferrer" class="text-blue-500 hover:underline">ghchart API</a> によって生成されています。</small>
        </p>
      </div>
    </div>
  </div>
</Layout>